/**
 * Representation of a CCD amount.
 * The base unit of CCD is micro CCD, which is the representation
 * used on chain.
 */
export declare class CcdAmount {
    microCcdAmount: bigint;
    constructor(microCcdAmount: bigint);
    toJSON(): string;
}
