/**
 * Representation of a GTU amount.
 * The base unit of GTU is micro GTU, which is the representation
 * used on chain.
 */
export declare class GtuAmount {
    microGtuAmount: bigint;
    constructor(microGtuAmount: bigint);
}
