import { push } from "../push.js";
/**
 * Shared helper function for `groupBy*` reducers
 *
 * @param opts -
 *
 * @internal
 */
export const __groupByOpts = (opts) => ({
    key: (x) => x,
    group: push(),
    ...opts,
});
